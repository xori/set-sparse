{
  "name": "set-sparse",
  "version": "1.0.0-alpha",
  "description": "Sets the sparse flag on files in Windows x86.",
  "main": "./src/index.js",
  "scripts": {
    "build": "node-gyp rebuild",
    "prepack": "cp ./build/Release/sparse.node ./src/sparse.node",
    "test": "tap --comments --no-coverage -R spec"
  },
  "keywords": ["windows", "sparse"],
  "author": "Evan Verworn <evan@verworn.ca>",
  "license": "MIT",
  "devDependencies": {
    "fswin": "^3.21.107",
    "node-gyp": "^7.1.2",
    "tap": "^14.11.0"
  }
}
